name: 0_IPTV检测全步骤

on:
  workflow_dispatch:  # 支持手动触发
  schedule:
    - cron: '05 16 * * *'  # 定时触发，UTC 16:05 即北京时间 00:05

permissions:
  contents: write  # 赋予写权限，允许提交和推送代码

jobs:
  download_all_m3u:
    runs-on: ubuntu-latest
    outputs:
      download_done: ${{ steps.set_done.outputs.done }}  # 输出标记，表示此job完成
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4  # 签出仓库代码
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v5  # 配置Python 3.11环境
        with:
          python-version: '3.11'

      - name: Install dependencies for download
        run: |
          python -m pip install --upgrade pip
          pip install requests  # 安装requests库，供下载脚本使用

      - name: Ensure output directories exist
        run: |
          mkdir -p input/source input/download/my input/download/network scripts  # 创建所需目录

      - name: Run M3U download script
        run: python scripts/1_download_m3u.py  # 执行下载M3U脚本

      - name: Commit & Push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add input/download  # 添加变更文件
          if git diff --cached --quiet; then
            echo "✅ No changes to commit."
            exit 0
          fi
          git commit -m "Update M3U sources [ci skip]"  # 提交变更，跳过CI
          if [ -n "$(git status --porcelain)" ]; then
            git stash push -m "ci-stash-before-pull"
            git pull --rebase origin main  # 先拉取最新代码，防止冲突
            git stash pop || echo "No stash to pop"
          else
            git pull --rebase origin main
          fi
          git push origin main  # 推送代码到远程仓库

      - id: set_done
        run: echo "::set-output name=done::true"  # 标记此job完成，用于下游依赖

  merge_local_sources:
    needs: download_all_m3u  # 依赖前一个job完成后执行
    runs-on: ubuntu-latest
    outputs:
      merge_done: ${{ steps.set_done.outputs.done }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # 签出代码
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies for merge
        run: |
          pip install --upgrade pip
          pip install requests chardet  # 安装依赖库

      - name: Run merge script
        run: python scripts/2_merge_local_sources.py  # 执行合并网络源脚本

      - name: Commit & Push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/  # 添加合并后的结果
          if ! git diff --cached --quiet; then
            git commit -m "🤖 Auto merge local sources $(date '+%Y-%m-%d %H:%M:%S')"
            if [ -n "$(git status --porcelain)" ]; then
              git stash push -m "ci-stash-before-pull"
              git pull --rebase origin main
              git stash pop || echo "No stash to pop"
            else
              git pull --rebase origin main
            fi
            git push origin main
          else
            echo "✅ No changes to commit."
          fi

      - id: set_done
        run: echo "::set-output name=done::true"

  split_merge_total:
    needs: merge_local_sources  # 依赖上一个job完成后执行
    runs-on: ubuntu-latest
    outputs:
      split_done: ${{ steps.set_done.outputs.done }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install chardet  # 安装chardet库

      - name: Run split script
        run: python scripts/3_split_merge.py  # 执行分割merge_total.csv脚本

      - name: Commit & Push chunks
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/middle/chunk/  # 添加分割结果
          if ! git diff --cached --quiet; then
            git commit -m "Auto split merge_total.csv into chunks [ci skip]"
            if [ -n "$(git status --porcelain)" ]; then
              git stash push -m "ci-stash-before-pull"
              git pull --rebase origin main
              git stash pop || echo "No stash to pop"
            else
              git pull --rebase origin main
            fi
            git push https://${PAT_TOKEN}@github.com/${{ github.repository }}.git HEAD:refs/heads/main
          else
            echo "✅ No changes to commit."
          fi

      - id: set_done
        run: echo "::set-output name=done::true"

  generate_chunk_workflow:
    needs: split_merge_total
    runs-on: ubuntu-latest
    outputs:
      gen_done: ${{ steps.set_done.outputs.done }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Git user and PAT
        env:
          PUSH_TOKEN1: ${{ secrets.PUSH_TOKEN1 }}
          REPO: ${{ github.repository }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${PUSH_TOKEN1}@github.com/${REPO}.git

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install chardet pillow tqdm requests  # 安装所需Python库

      - name: Generate and Push workflows
        run: |
          python scripts/4_generate_chunk_workflows.py  # 生成chunk工作流脚本
          git add .github/workflows output/middle
          if ! git diff --cached --quiet; then
            git commit -m "ci: auto-generate deep validation workflows"
            if [ -n "$(git status --porcelain)" ]; then
              git stash push -m "ci-stash-before-pull"
              git pull --rebase origin main
              git stash pop || echo "No stash to pop"
            else
              git pull --rebase origin main
            fi
            git push origin main
          else
            echo "无更改，跳过提交和推送"
          fi

      - id: set_done
        run: echo "::set-output name=done::true"

  wait_for_scan:
    needs: generate_chunk_workflow
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.PUSH_TOKEN1 }}
      REPO_OWNER: niuinyang
      REPO_NAME: niuin_iptv
    outputs:
      wait_done: ${{ steps.set_done.outputs.done }}
    steps:
      - uses: actions/checkout@v3  # 签出代码

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install aiohttp requests  # 安装异步http依赖

      - name: Run scan watcher (async & realtime output)
        env:
          GITHUB_TOKEN: ${{ secrets.PUSH_TOKEN1 }}
          REPO_OWNER: niuinyang
          REPO_NAME: niuin_iptv
          PYTHONUNBUFFERED: "1"  # 使Python实时打印日志
        run: |
          python -u scripts/5.4_check-scan-finished-async.py  # 异步扫描完成检测脚本

      - id: set_done
        run: echo "::set-output name=done::true"

  delete_workflows:
    needs: wait_for_scan
    runs-on: ubuntu-latest
    env:
      PUSH_TOKEN: ${{ secrets.PUSH_TOKEN1 }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Delete scan_chunk workflow files only
        run: |
          FILES_SCAN_CHUNK=$(find .github/workflows -type f -name "scan_chunk*.yml" || true)
          if [ -z "$FILES_SCAN_CHUNK" ]; then
            echo "No matching sash-chunk workflow files found."
            exit 0
          fi
          echo "Deleting files:"
          echo "$FILES_SCAN_CHUNK"
          git rm $FILES_SCAN_CHUNK || true  # 删除生成的chunk workflow文件
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -m "Delete scan_chunk workflow files" || echo "No changes to commit"
          git remote set-url origin https://x-access-token:${PUSH_TOKEN}@github.com/niuinyang/niuin_iptv.git
          n=0
          until [ $n -ge 3 ]
          do
            git pull --rebase origin main
            git push origin HEAD:main && break
            n=$((n+1))
            echo "Push failed, retry $n..."
            sleep 5
          done

  merge_and_push:
    needs: delete_workflows
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas  # 安装pandas库

      - name: Run merge script
        run: python scripts/6_merge_scan_result.py  # 合并扫描结果脚本

      - name: Commit and push changes with stash and retry
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/middle/working.csv 
          if ! git diff --cached --quiet; then
            git commit -m "Auto merge CSV and M3U files [skip ci]"
            MAX_RETRIES=3
            for i in $(seq 1 $MAX_RETRIES); do
              echo "Attempt $i to push..."
              if git push origin main --quiet; then
                echo "Push succeeded"
                break
              else
                echo "Push failed, trying stash and pull..."
                if [ -n "$(git status --porcelain)" ]; then
                  git stash push -m "ci stash before pull"
                  git pull --rebase origin main --quiet
                  git stash pop || echo "No stash to pop"
                else
                  git pull --rebase origin main --quiet
                fi
              fi
              if [ "$i" -eq "$MAX_RETRIES" ]; then
                echo "Max retries reached, push failed"
                exit 1
              fi
            done
          else
            echo "No changes to commit."
          fi

  standardize:
    needs: merge_and_push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Clone iptv-org/database repository
        run: |
          mkdir -p input/iptv-org
          git clone --depth=1 https://github.com/iptv-org/database.git input/iptv-org/database  # 克隆第三方iptv-org数据库

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # 安装脚本依赖

      - name: Run IPTV Standardization Script
        run: python scripts/7_standardize_names.py  # 运行标准化脚本

      - name: Commit and Push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/total_final.csv input/channel_data.csv input/channel_data.xlsx
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Update total_final.csv and channel_data.csv via workflow"
          retries=3
          count=0
          until git push; do
            echo "Push failed, attempting to rebase (try #$((count+1)))"
            git pull --rebase --autostash
            count=$((count+1))
            if [ $count -ge $retries ]; then
              echo "Push failed after $retries attempts"
              exit 1
            fi
          done

  generate_m3u:
    needs: standardize
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install aiohttp pypinyin aiofiles  # 安装m3u生成脚本依赖

      - name: Run m3u generation script
        run: python scripts/8_cvs_to_m3u.py  # 生成M3U文件

      - name: Commit and push changes with stash and retry
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/dxl.m3u output/sjmz.m3u
          if ! git diff --cached --quiet; then
            git commit -m "Auto update m3u and unicast cache [skip ci]"
            MAX_RETRIES=3
            for i in $(seq 1 $MAX_RETRIES); do
              echo "Attempt $i to push..."
              if git push origin main --quiet; then
                echo "Push succeeded"
                break
              else
                echo "Push failed, pulling remote..."
                if [ -n "$(git status --porcelain)" ]; then
                  git stash push -m "ci stash before pull"
                  git pull --rebase origin main --quiet
                  git stash pop || echo "No stash to pop"
                else
                  git pull --rebase origin main --quiet
                fi
              fi
              if [ "$i" -eq "$MAX_RETRIES" ]; then
                echo "Max retries reached"
                exit 1
              fi
            done
          else
            echo "No changes to commit."
          fi
