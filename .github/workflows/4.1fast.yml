name: IPTV Fast Scan (宽松模式)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fast_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install aiohttp tqdm

      - name: Create output directory
        run: mkdir -p output/middle

      - name: Run Fast Scan (宽松模式)
        run: |
          echo "🚀 开始 IPTV 快速检测 (宽松模式)..."
          PYTHONUNBUFFERED=1 stdbuf -oL python scripts/4.1fast_scan.py \
            --input output/merge_total.csv \
            --output output/middle/fast_scan.csv \
            --invalid output/middle/fast_scan_invalid.csv \
            --concurrency 100 \
            --timeout 12
          echo "✅ 快速检测完成"

      - name: Show sample results
        run: |
          echo "📊 有效源预览："
          head -10 output/middle/fast_scan.csv || true
          echo ""
          echo "⚠️ 无效源预览："
          head -10 output/middle/fast_scan_invalid.csv || true

      - name: Commit and push results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add output/middle/fast_scan.csv output/middle/fast_scan_invalid.csv
          git commit -m "Update fast scan results [ci skip]" || echo "No changes to commit"
          git push
