name: IPTV Fast Scan

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fast_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install aiohttp tqdm

      - name: Create output directory
        run: mkdir -p output/middle

      - name: Run Fast Scan (宽松模式)
        run: |
          echo "🚀 开始 IPTV 快速检测 (宽松模式)..."
          python scripts/4.1fast_scan.py \
            --input output/merge_total.csv \
            --output output/middle/fast_scan.csv \
            --invalid output/middle/fast_scan_invalid.csv \
            --concurrency 100 \
            --timeout 15
          echo "✅ 快速检测完成"

      - name: Show valid results sample
        run: |
          echo "📄 有效源样例 (前10行):"
          head -10 output/middle/fast_scan.csv || echo "⚠️ 未找到有效源文件"

      - name: Show invalid results sample
        run: |
          echo "❌ 无效源样例 (前10行):"
          head -10 output/middle/fast_scan_invalid.csv || echo "⚠️ 未找到无效源文件"

      - name: Summary Statistics
        run: |
          echo "📊 检测结果统计："
          VALID_COUNT=$(($(wc -l < output/middle/fast_scan.csv)-1))
          INVALID_COUNT=$(($(wc -l < output/middle/fast_scan_invalid.csv)-1))
          TOTAL=$((VALID_COUNT + INVALID_COUNT))
          echo "有效源数量: ${VALID_COUNT}"
          echo "无效源数量: ${INVALID_COUNT}"
          echo "总检测数量: ${TOTAL}"

      - name: Commit and Push Results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add output/middle/fast_scan.csv output/middle/fast_scan_invalid.csv
          git commit -m "Update fast scan results [ci skip]" || echo "No changes to commit"
          git push