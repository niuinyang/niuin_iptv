name: IPTV Fast Scan (宽松模式)

on:
  schedule:
    # 每天 02:30 Asia/Taipei (UTC+8) => UTC 前一天 18:30
    - cron: '30 18 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fast_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 拉取完整历史，便于 rebase 合并

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install aiohttp tqdm

      - name: Create output directory
        run: mkdir -p output/middle

      - name: Run Fast Scan (宽松模式)
        run: |
          echo "🚀 开始 IPTV 快速检测 (宽松模式)..."
          PYTHONUNBUFFERED=1 stdbuf -oL python scripts/4.1fast_scan.py \
            --input output/merge_total.csv \
            --output output/middle/fast_scan.csv \
            --invalid output/middle/fast_scan_invalid.csv \
            --concurrency 100 \
            --timeout 12
          echo "✅ 快速检测完成"

      - name: Show sample results
        run: |
          echo "📊 有效源预览："
          head -10 output/middle/fast_scan.csv || true
          echo ""
          echo "⚠️ 无效源预览："
          head -10 output/middle/fast_scan_invalid.csv || true

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Pull latest changes (merge remote updates)
        run: |
          git fetch origin main
          git pull --rebase origin main || git rebase --abort

      - name: Commit and push results
        run: |
          git add output/middle/fast_scan.csv output/middle/fast_scan_invalid.csv 2>/dev/null || true
          if ! git diff --cached --quiet; then
            git commit -m "Update fast scan results [ci skip]"
            git push origin HEAD:main
          else
            echo "✅ No changes to commit."
          fi
