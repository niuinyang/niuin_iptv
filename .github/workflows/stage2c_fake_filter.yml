name: Stage 2C - Final Check

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-stage2c:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6小时

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install tqdm aiohttp ffmpeg-python requests

    - name: Run Stage 2C Final Check
      run: python scripts/stage2c_final_check.py

    - name: Commit and Push Results
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add output/middle/stage2c_final.csv
        git commit -m "Update stage2c_final.csv [skip ci]" || echo "No changes to commit"
        git push
